Metadata-Version: 2.4
Name: yolo-service
Version: 0.1.0
Summary: Add your description here
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: colorlog>=6.10.1
Requires-Dist: pyyaml>=6.0.3
Requires-Dist: fastapi>=0.135.2
Requires-Dist: pydantic-settings>=2.13.1
Requires-Dist: ultralytics
Requires-Dist: uvicorn>=0.42.0
Requires-Dist: pathspec>=1.0.4
Requires-Dist: setuptools>=81.0.0
Requires-Dist: python-multipart>=0.0.22
Requires-Dist: minio>=7.2.20
Requires-Dist: moviepy>=2.2.1
Requires-Dist: onnx>=1.16.0
Requires-Dist: onnxruntime<1.24.0,>=1.18.0; python_version < "3.11" and platform_machine == "AMD64" and sys_platform == "win32"
Requires-Dist: onnxruntime<1.24.0,>=1.18.0; python_version < "3.11" and platform_machine == "x86_64" and sys_platform == "linux"
Requires-Dist: onnxruntime>=1.24.0; python_version >= "3.11" and platform_machine == "AMD64" and sys_platform == "win32"
Requires-Dist: onnxruntime>=1.24.0; python_version >= "3.11" and platform_machine == "x86_64" and sys_platform == "linux"
Provides-Extra: local-gpu
Requires-Dist: onnxruntime-gpu>=1.18.0; (platform_machine == "AMD64" and sys_platform == "win32") and extra == "local-gpu"
Requires-Dist: onnxruntime-gpu>=1.18.0; (platform_machine == "x86_64" and sys_platform == "linux") and extra == "local-gpu"

# UAV 车辆检测微服务 (YOLO Service)

基于 **FastAPI** 和 **YOLOv8** 模型构建的无人机视角目标检测微服务。该项目支持对上传的图像和视频流进行基于 YOLO 的 AI 目标识别任务，自动叠加检测框并归档渲染结果，同时对外提供静态资源访问链接。

---

## 核心特性

- **高性能推理**：集成并支持最新的 ultralytics YOLO 模型架构（支持自训练模型权重）。
- **多权重格式**：支持 `.pt`、`.onnx` 两种模型权重格式（按运行环境依赖自动校验）。
- **全格式支持**：支持单张图片识别及常见视频格式（`.mp4`, `.avi`, `.mov`, `.mkv`）的异步检测。
- **文件存储规范化**：采用基于 UUID 的无碰撞文件存储策略，物理隔离上传的临时源文件与渲染后的输出媒体。
- **自动挂载与服务**：FastAPI 内置的静态文件拦截机制自动暴露图片和转码后的视频物理存放路径映射。
- **配置驱动**：环境敏感参数（端口、路径、模型模型权重）等均可通过 `.env` 文件进行灵活配置。

---

## 技术栈

- **语言**: Python 3.10+
- **后端框架**: FastAPI, Uvicorn
- **AI 引擎**: PyTorch, Ultralytics, OpenCV
- **包管理**: [uv](https://github.com/astral-sh/uv)
- **配置管理**: Pydantic Settings

---

## 项目关键目录结构

```text
yolo-service/
├── src/yoloservice/
│   ├── app/                 # FastAPI 应用与生命周期、路由挂载
│   ├── common/              # 公共拦截器、配置参数、工具类 (如 UUID 文件处理)
│   ├── config/              # 环境与设置模型
│   ├── core/                # YOLO 核心单例管理器 (yolo_manager)
│   ├── service/             # 推理核心逻辑层（处理图片和视频的分析与存储）
│   ├── web/                 # API 注册处（图片 Router，视频 Router）
│   └── main.py              # 服务入口文件
├── data/                    # 本地数据存储目录（根据 .env 自动生成）
│   ├── temp_uploads/        # 用户上传的源文件临时存放区（处理完后自动清理）
│   └── outputs/             # 推理结果导出区
│       ├── images/          # 画框后的图片资源
│       └── videos/          # 渲染后的预测视频 (转存处理)
├── .env                     # 环境配置文件
└── pyproject.toml / uv.lock # 依赖配置文件
```

---

## 快速开始

### 1. 环境准备及依赖安装

确保已在系统中安装了 `uv` 包管理器：

```powershell
powershell -c "irm https://astral-sh.uv.io/install.ps1 | iex"
```

拉取并安装工程依赖：

```powershell
uv venv
uv sync
```

### 2. 环境配置 (`.env`)

确保项目根目录下有以下 `.env` 配置文件（如需要，可做动态调整）：

```ini
# 服务基础配置
PROJECT_NAME="UAV Detection Service [DEV]"
APP_HOST=0.0.0.0
APP_PORT=8000
API_PREFIX="/api/v1"
DEBUG=true

# 模型配置 (将对应的 .pt 文件放置在对应位置)
MODEL_WEIGHTS_PATH="weights/yolo26n.pt"

# 目录配置
UPLOAD_DIR="data/temp_uploads"
OUTPUT_DIR="data/outputs"
```

可选示例：

```ini
MODEL_WEIGHTS_PATH="weights/sky-scan-det_640_fp16.onnx"
```

### 2.1 权重格式依赖说明

- `.pt`：默认依赖即可运行。
- `.onnx`：需安装 `onnx` 与 `onnxruntime`（本项目已在依赖中声明）。

推荐安装方式（Windows/Linux 通用）：

```powershell
# 基础（.pt + .onnx）
uv sync

# 可选：ONNX GPU 推理
uv sync --extra local-gpu
```

### 3. 启动服务

通过 Uvicorn 运行项目（开发模式开启热更新）：

```powershell
uv run python src/yoloservice/main.py
```

_(Windows 下如因依赖无法自动识别，也可激活虚拟环境直接使用 `python src/yoloservice/main.py`)_

---

## 🔌 接口文档 (API Endpoints)

当服务成功启动后，浏览器打开 **`http://127.0.0.1:8000/docs`** 即可查看交互式 Swagger 文档。

### 1. 图像检测 (Image Predict)

- **POST** `/api/v1/image/predict`
- **参数**: `multipart/form-data` → `file: File`
- **响应**:
  - 返回识别到边界框（bbox）、类别数量及置信度信息。
  - 附带渲染后图片的永久访问链接：`http://.../static/images/image_<UUID>.jpg`

### 2. 视频检测 (Video Predict)

- **POST** `/api/v1/video/predict`
- **参数**: `multipart/form-data` → `file: File` (仅支持常见主流视频格式)
- **响应**:
  - 异步处理上传的任务，自动隔离存储并清除临时文件。
  - 返回渲染后视频的永久访问链接：`http://.../static/videos/video_<UUID>.mp4`

> **注意**：原生 YOLO OpenCV 导出的视频格式可能包含 MP4V 编码。如果不通过工具进行 H264 转码，现代浏览器原生 HTML5 标签可能提示无法解析这部分老旧或部分特殊的视频文件。这种情况推荐下载或调用本地播放器浏览。

---

## 生产环境部署 (Linux / Ubuntu)

对于生产环境（如 Ubuntu 22.04），除了基础的环境配置，还需要注意 OpenCV 的系统级依赖以及后台常驻运行机制。

### 1. 安装系统依赖库

OpenCV 在 Linux 下运行通常需要系统依赖，请首先执行：

```bash
sudo apt-get update
sudo apt-get install -y libgl1 libglib2.0-0
```

### 2. 安装 uv 与同步项目

```bash
# Linux 下使用 curl 安装 uv
curl -LsSf https://astral.sh/uv/install.sh | sh
source $HOME/.cargo/env

# 进入项目目录并同步依赖
cd /path/to/yolo-service
uv venv
uv sync
```

### 3. 修改生产环境变量

拷贝一份 `.env` 文件并将 `DEBUG` 设置为 `false`，同时根据服务器情况配置端口：

```ini
# .env
PROJECT_NAME="UAV Detection Service [PROD]"
APP_HOST=0.0.0.0
APP_PORT=8000
DEBUG=false
WORKERS=4  # 生产环境建议开启多进程，数值通常设置为 CPU 核心数
```

### 4. GPU 加速 (可选但推荐)

如果在带有 NVIDIA 显卡的 Linux 服务器上运行，确保已安装正确的 Nvidia 驱动及 CUDA Toolkit。由于 `uv sync` 默认可能拉取 CPU 版本的 PyTorch，你可能需要手动指定或覆盖 PyTorch 安装为 cu118 / cu121 版本以激活 GPU 加速：

```bash
uv pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```

### 5. 后台运行服务

推荐使用 `systemd`、`Supervisor`、或 `pm2` 等守护进程工具来管理微服务。最基础的后台运行命令（结合 `nohup`）：

```bash
nohup uv run python src/yoloservice/main.py > yolo-service.log 2>&1 &
```

---

## 进阶开发建议

- **路由拓展**：可通过在 `src/yoloservice/web/router` 内增加文件并挂载 `@register_router` 装饰器实现路由自动发现。
- **自定义预处理**：在 `process_image` 或 `process_video` (位于 `inference_service.py`) 层编写定制的 OpenCV OpenCV 图片变换处理。
